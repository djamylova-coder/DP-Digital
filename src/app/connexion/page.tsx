'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export default function Connexion(){const [code,setCode]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();
 async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({code})});const d=await r.json();if(r.ok)router.replace('/dashboard');else setError(d.message||'Accès refusé');setLoading(false)}
 return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="card w-full max-w-md p-8 space-y-6"><div><p className="text-sky-400 text-sm font-semibold">DP DIGITAL</p><h1 className="text-3xl font-bold mt-2">Administration</h1><p className="muted mt-2">Connexion administrateur</p></div><input autoFocus value={code} onChange={e=>setCode(e.target.value)} type="password" autoComplete="current-password" placeholder="Code d’accès" className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none focus:border-sky-400"/><button disabled={loading||!code} className="w-full rounded-xl bg-sky-400 text-slate-950 font-bold py-3 disabled:opacity-50">{loading?'Connexion…':'Se connecter'}</button>{error&&<p className="text-red-300 text-sm">{error}</p>}</form></main>}
