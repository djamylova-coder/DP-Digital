import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {requireAdmin} from '@/lib/api-auth';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const a=await requireAdmin();if(a.response)return a.response;const {id}=await params;const b=await req.json();const data:any={};for(const k of ['operateur','categorie','nom','validite','typeSyntaxe','syntaxe'])if(b[k]!==undefined)data[k]=String(b[k]);if(b.prixFcfa!==undefined)data.prixFcfa=Number(b.prixFcfa);if(b.actif!==undefined)data.actif=Boolean(b.actif);if(b.statutVerification!==undefined)data.statutVerification=b.statutVerification==='verifie'?'verifie':'a_tester';const row=await prisma.$transaction(async tx=>{const r=await tx.forfait.update({where:{id},data});await tx.audit.create({data:{compteId:a.session!.compteId,action:'MODIFICATION_FORFAIT',details:JSON.stringify({id,data})}});return r});return NextResponse.json(row)}
