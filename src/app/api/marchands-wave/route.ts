import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {requireAdmin} from '@/lib/api-auth';
export async function GET(){const a=await requireAdmin();if(a.response)return a.response;const [marchands,nonRapprochees]=await Promise.all([prisma.marchandWave.findMany({select:{id:true,nom:true,reference:true,actif:true}}),prisma.notificationWave.findMany({where:{rapprochee:false},orderBy:{dateReception:'desc'},take:50})]);return NextResponse.json({marchands,nonRapprochees})}
